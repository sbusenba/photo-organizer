import "./Status.css";

const Status = ({ lastUpdated }) => {
    return (
        <div>
            <button onClick={()=>{}}><h2>Scan</h2>
            <p>Last Updated: {lastUpdated}</p>
            </button>
        </div>
    );
};

export default Status;